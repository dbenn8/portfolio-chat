I understand your business problem. Then I build the solution. In days, not quarters.
      
      
        I'm a product manager and co-founder who builds with AI. My background is in solution engineering and product management — understanding what businesses actually need, gathering requirements, and turning ambiguity into clear specs. The difference now: I don't just write the spec and hand it off. I prototype the solution myself, prove it works, and deliver something you can touch, test, and iterate on — fast.
      
      
        I've shipped production apps, built autonomous AI agent systems, and prototyped tools that solve real problems for real users. Some of these are polished. Some are scrappy. All of them went from "I have an idea" to "here, try this" in days, not months.
      
    

    
    
      The Acceleration Curve
      
        Same person. Better tools. The projects got more ambitious while the timelines got shorter. All dates verified from git commit history.
      

      
        {[
          { date: 'Oct 2023', name: 'Kumpel.ai', duration: '~2 months', tags: ['Python', 'Django', 'OpenAI'] },
          { date: 'May 2024', name: 'Burrfect Auto-Timer', duration: '6 days', tags: ['Flutter', 'Audio'] },
          { date: 'Oct 2025', name: 'BTC Backtesting Engine', duration: '8 days', tags: ['Python', 'Claude', 'Plotly'] },
          { date: 'Nov 2025', name: 'claude-slack', duration: '3 days (core)', tags: ['Node.js', 'Claude Code', 'Slack API'] },
          { date: 'Feb 2026', name: 'Woordjes', duration: '4 days → production', tags: ['Django', 'Claude API', 'Appliku'] },
          { date: 'Feb 2026', name: 'Project Orchestrator', duration: '3 days', tags: ['Claude Code', 'Vagrant', 'TDD'] },
          { date: 'Feb 2026', name: 'Audio Interface', duration: '2 days', tags: ['Claude Code', 'Audio'] },
        ].map((item) => (
          
            {item.date}
            
              {item.name}
              
                {item.tags.map((tag) => (
                  
                    {tag}
                  
                ))}
              
              {item.duration}
            
          
        ))}
      
    

    
    
      Featured Projects

      
        {projects.map((project) => (
          
            
              {project.name}
              
                {project.status}
              
            

            {project.hook}

            
              {project.tags.map((tag) => (
                
                  {tag}
                
              ))}
            

            
              Read the full story →
            
          
        ))}
      
    

    
    
      More Projects

      
        {secondaryProjects.map((project) => (
          
            
              {project.name}
              
                {project.status}
              
            
            {project.description}
            
              {project.tags.map((tag) => (
                {tag}
              ))}
            
          
        ))}
      
    

    
    
      
        
          Hiring?
          
            I bring product thinking, AI fluency, and the ability to actually build the thing. I've managed products, gathered requirements from stakeholders, written specs, and — increasingly — prototyped and shipped the solutions myself.
          
          
            Let's talk on LinkedIn →
          
        

        
          Need something built?
          
            I can take your business problem from napkin sketch to working prototype faster than you'd expect. Whether it's a new feature, a full application, or an AI system that does something new — let's talk about what you need.
          
          
            Email me →